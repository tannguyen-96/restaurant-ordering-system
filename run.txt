backend
# Run the default application
./gradlew bootRun
./gradlew local # Run with profile local

# Run the application with a specific profile (e.g., local)
./gradlew bootRun --args='--spring.profiles.active=local'

# link swagger-ui
http://localhost:8080/swagger-ui.html

frontend
npm start (node24)
http://localhost:3000